# MySQL 8.4 tuned for a 2 GB box shared with Node, Redis and Caddy.

[mysqld]

# The single most important setting here, and the one this box was wrong about
# twice.
#
# 768M does not fit. Measured on 2026-09-23 with the API, Redis and Caddy
# alongside it on 1,835 MB: mysqld ended up with 257-826 MB in swap, and a
# swapped buffer-pool page is a disk read wearing a cache hit as a disguise. It
# showed as tail latency rather than average -- SELECT 1 at p50 2.96 ms but p95
# 9.09 ms, a COUNT at p50 1.92 ms but p95 69.35 ms.
#
# 512M does fit, and holds everything that matters. Of the ~1.2 GB database,
# every table except TestCase totals 46 MB. TestCase is read only by the judge,
# one problem at a time (~5,000 cases, ~1.1 MB), through the 64-problem LRU in
# lib/test-suite-cache.ts. With 768M configured, InnoDB was holding just 381 MB
# of data and leaving 24,761 pages free -- it never wanted the rest.
#
# An earlier attempt at 512M was reverted as a regression. That was a
# misreading: mysqld was 826 MB into swap at the time, and the pool size was
# not what was slow. Tell those two apart before changing this again -- read
# mysqld_swap_mb from deploy/track-resources.sh first, and only then the hit
# rate. A hit rate cannot see whether the page it hit was in RAM or in swap.
innodb_buffer_pool_size = 512M
innodb_buffer_pool_instances = 1

# Redo log. The default flushes constantly under a seed run
# (scripts/seed-catalog.ts writes millions of TestCase rows); 256 MB rides it out.
innodb_redo_log_capacity = 256M

# The app reaches MySQL over the compose network, never a socket, and one API
# instance opens `connection_limit` from DATABASE_URL (10). The rest of the
# headroom is for the content scripts run from a workstation.
max_connections = 60

# Must match the source server (Percona 8.0.46 reports 0) and can never be
# changed after the data directory is initialised. The schema is CamelCase --
# `User`, `Submission`, `TestCase` -- so a server that folded names to
# lowercase would break every Prisma query on this database.
lower_case_table_names = 0

# Matched to the source exactly rather than to a personal preference. The
# catalogue filters rely on case-insensitive comparison
# (`where.difficulty = { equals: difficulty }` in routes/problems.ts relies on
# the CI collation), and 0900_ai_ci is what those rows were written under.
character-set-server = utf8mb4
collation-server = utf8mb4_0900_ai_ci

# NOTE: no default_authentication_plugin here. It was removed in MySQL 8.4 and
# naming it stops the server booting at all. caching_sha2_password is the
# default anyway, and lib/prisma.ts already sets allowPublicKeyRetrieval so
# the MariaDB connector can complete that handshake without TLS.

# Slow-query visibility without the overhead of the general log. 500 ms is
# well above anything this schema should take now that the DB is local -- on
# the old shared host a bare `SELECT 1` cost 269 ms of wire time.
slow_query_log = 1
slow_query_log_file = /var/lib/mysql/slow.log
long_query_time = 0.5

# A local disk on a small instance is not a RAID array; 1 is the safe default
# and the write volume here does not justify relaxing it. The bulk restore in
# deploy/README.md relaxes it to 2 for the duration and puts it back.
innodb_flush_log_at_trx_commit = 1
