"use strict";
const lines = require("fs").readFileSync(0, "utf8").split("\n").filter((l) => l.trim() !== "");
const rules = [
  // TODO: [regex, category, hint] in priority order
];
for (const line of lines) {
  // TODO
}
